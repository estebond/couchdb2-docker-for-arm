sudo: required

services:
  - docker

env:
  - RELEASES=2.2.0

script:
  - for rel in $RELEASES; do docker build -t couchdb2-arm-debian $rel; docker run -t couchdb2-arm-debian -p 5984:5984 && sleep 10 && curl http://localhost:5984 && docker kill $rel; done
